# Copy to .env.local and set your deployed API Gateway base URL.
# Only VITE_-prefixed vars are exposed to the browser. Never put secrets here.
VITE_API_BASE=https://your-api-id.execute-api.ap-south-1.amazonaws.com
